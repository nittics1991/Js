/**
*   テンプレート
*
*   @param 
*/
let TemplateBuilder = function(template) {
    this._template = template;
    //??
    
    
};

/**
*   展開
*
*   @param array definition
*   @return string
*/
TemplateBuilder.prototype.execute = function(definition = []) {
    
};

TemplateBuilder.prototype._build = function(definition = []) {
    
    let _contents = definition.map(function(val, index. array) {
        
        if () {
            
        else if () {
            
        else if () {
            
        } else {
            throw "type error:" + val
        }
        
    });
    
    
    
    
};


